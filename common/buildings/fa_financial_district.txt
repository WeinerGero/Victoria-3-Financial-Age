# Файл: fa_financial_district.txt (Версия 1.2)
# Перезаписывает ванильное здание. Используем ванильные и новые группы ПМ.

building_financial_district = {
    # --- Базовые параметры ---
    building_group = bg_financial_districts
    city_type = city
    levels_per_mesh = 10
    icon = "gfx/interface/icons/building_icons/building_financial_district.dds"
    lens = infrastructure
    background = "gfx/interface/icons/building_icons/backgrounds/building_panel_bg_monuments.dds"

    # --- НАШИ ИЗМЕНЕНИЯ ---
    buildable = yes
    expandable = yes
    downsizeable = yes
    required_construction = construction_cost_very_high

    # --- Группы Производственных Методов ---
    # Мы указываем игре, какие группы ПМ должно использовать это здание.
    production_method_groups = {
        pmg_fa_banking_operations             # Наша новая группа для банковских/биржевых операций
        pmg_ownership_building_financial_district # ВАНИЛЬНАЯ группа для методов владения
    }


	investment_scores = {
		bg_manufacturing_score = {
			group = "bg_manufacturing"
			score = {
				value = 1

				if = {
					limit = {
						has_law = law_type:law_traditionalism
					}
					multiply = 0.1
				}
				else_if = {
					limit = {
						OR = {
							has_law = law_type:law_industry_banned
							has_law = law_type:law_extraction_economy
						}
					}
					multiply = 0.5
				}
				else_if = {
					limit = {
						has_law = law_type:law_agrarianism
					}
					multiply = 0.75
				}
			}
		}
		bg_manufacturing_score = {
			group = "bg_trade"
			score = {
				value = 1.0
			}
		}
		bg_agriculture_score = {
			group = "bg_agriculture"
			score = {
				value = 0.5

				if = {
					limit = {
						has_law = law_type:law_commercialized_agriculture
					}
					add = 0.5
				}
			}
		}
		bg_ranching_score = {
			group = "bg_ranching"
			score = {
				value = 0.5

				if = {
					limit = {
						has_law = law_type:law_traditionalism
					}
					multiply = 0.5
				}

				if = {
					limit = {
						has_law = law_type:law_commercialized_agriculture
					}
					add = 0.5
				}
			}
		}
		bg_plantations_score = {
			group = "bg_plantations"
			score = {
				value = 0.75

				if = {
					limit = {
						has_law = law_type:law_traditionalism
					}
					multiply = 0.5
				}

				if = {
					limit = {
						has_law = law_type:law_commercialized_agriculture
					}
					add = 0.25
				}
			}
		}
		bg_arts_score = {
			group = "bg_arts"
			score = {
				value = 0.5

				if = {
					limit = {
						has_law = law_type:law_traditionalism
					}
					multiply = 0.5
				}
			}
		}
		bg_mining_score = {
			group = "bg_mining"
			score = {
				value = 1

				if = {
					limit = {
						has_law = law_type:law_traditionalism
					}
					multiply = 0.25
				}
				else_if = {
					limit = {
						has_law = law_type:law_industry_banned
					}
					multiply = 0.5
				}
				else_if = {
					limit = {
						has_law = law_type:law_agrarianism
					}
					multiply = 0.75
				}
			}
		}
		bg_logging_score = {
			group = "bg_logging"
			score = {
				value = 1

				if = {
					limit = {
						has_law = law_type:law_traditionalism
					}
					multiply = 0.5
				}
			}
		}
		bg_rubber_score = {
			group = "bg_rubber"
			score = {
				value = 1

				if = {
					limit = {
						has_law = law_type:law_traditionalism
					}
					multiply = 0.5
				}
			}
		}
		bg_whaling_score = {
			group = "bg_whaling"
			score = {
				value = 1

				if = {
					limit = {
						has_law = law_type:law_traditionalism
					}
					multiply = 0.5
				}
			}
		}
		bg_fishing_score = {
			group = "bg_fishing"
			score = {
				value = 1

				if = {
					limit = {
						has_law = law_type:law_traditionalism
					}
					multiply = 0.5
				}
			}
		}
		bg_oil_extraction_score = {
			group = "bg_oil_extraction"
			score = {
				value = 1

				if = {
					limit = {
						has_law = law_type:law_traditionalism
					}
					multiply = 0.1
				}
				else_if = {
					limit = {
						has_law = law_type:law_industry_banned
					}
					multiply = 0.5
				}
				else_if = {
					limit = {
						has_law = law_type:law_agrarianism
					}
					multiply = 0.75
				}
			}
		}
		bg_power_score = {
			group = "bg_power"
			score = {
				value = 1

				if = {
					limit = {
						has_law = law_type:law_traditionalism
					}
					multiply = 0.1
				}
				else_if = {
					limit = {
						OR = {
							has_law = law_type:law_industry_banned
							has_law = law_type:law_extraction_economy
						}
					}
					multiply = 0.5
				}
				else_if = {
					limit = {
						has_law = law_type:law_agrarianism
					}
					multiply = 0.75
				}
			}
		}
		bg_infrastructure_score = {
			group = "bg_infrastructure"
			score = {
				value = 1

				if = {
					limit = {
						has_law = law_type:law_traditionalism
					}
					multiply = 0.1
				}
				else_if = {
					limit = {
						has_law = law_type:law_industry_banned
					}
					multiply = 0.5
				}
				else_if = {
					limit = {
						has_law = law_type:law_agrarianism
					}
					multiply = 0.75
				}
			}
		}
	}
}